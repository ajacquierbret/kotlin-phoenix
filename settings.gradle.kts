pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "kotlin-phoenix"
include(":kotlinphoenix-client")
include(":kotlinphoenix-adapters")
